// This file exports all nodes which will be included in the package
// Export nodes directly
const { Blockfrost } = require('./nodes/Blockfrost/index');
const { BlockfrostApi } = require('./nodes/Blockfrost/Blockfrost.credentials');

module.exports = {
    nodes: [
        Blockfrost,
    ],
    credentials: [
        BlockfrostApi,
    ],
};